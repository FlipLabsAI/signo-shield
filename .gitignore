# Foundry build output. `abi/` is CHECKED IN on purpose — see tools/export-artifacts.py:
# the repository ships ABIs a reader can use without running the toolchain.
out/
cache/


# lib/ is NOT ignored: forge-std is a pinned git submodule so a clean
# `git clone --recursive` builds with no network fetch beyond the clone.
docs/book/

.env
.env.*
!.env.example

.DS_Store
node_modules/
broadcast/**/dry-run/
